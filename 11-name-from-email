public String nameFromEmail(String email) {
    return email.substring(0,email.indexOfIgnoreCase('.')).toLowerCase().capitalize() + ' ' +
    email.substring(email.indexOfIgnoreCase('.')+1,email.indexOfIgnoreCase('@')).toLowerCase().capitalize();
}