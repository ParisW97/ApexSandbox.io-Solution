public String convert15to18DigitId(String fifteenDigit) {
    if (fifteenDigit == null) {
        // no string given
        return null;
    }
    else if (fifteenDigit != 15) {
        // incorrect string length given
        return '-1';
    }
    return String.valueOf(Id.valueOf(fifteenDigit));
}